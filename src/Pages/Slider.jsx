import HeroSlider, {Slider} from 'hero-slider'

const Slider = () => {
    return (
        <div>
            <HeroSlider>
                <Slider>
                    background=
                </Slider>

            </HeroSlider>
        </div>
    );
};

export default Slider;